SVM trained on geometrical features (global features) + SVM trained on local features (found using Discriminative common vector [DCV] and Discrete Wavelet Feature Transform [DWT])
The use of two SVMs means the classification process is probably quiet slow
